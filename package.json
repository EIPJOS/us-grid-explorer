{
  "name": "us-grid-explorer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:state-pages": "node scripts/generate-state-pages.mjs",
    "generate:trust-pages": "node scripts/generate-trust-pages.mjs",
    "generate:guide-pages": "node scripts/generate-guide-pages.mjs",
    "generate:pages": "npm run generate:state-pages && npm run generate:trust-pages && npm run generate:guide-pages",
    "predev": "npm run generate:pages",
    "dev": "vite",
    "prebuild": "npm run generate:pages",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "leaflet": "^1.9.4",
    "lucide-react": "^0.468.0",
    "openai": "^6.44.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-leaflet": "^5.0.0",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  }
}
